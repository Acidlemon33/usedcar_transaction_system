spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/usedcar_transaction_system?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
    username: root
    password: 7622661

  jackson:
    # 关闭SNAKE_CASE策略，让前后端统一使用camelCase（小驼峰）
    # SNAKE_CASE 会导致前端发送的 realName 无法映射到 UserPO.realName (需要 real_name)
    # property-naming-strategy: SNAKE_CASE

mybatis-plus:
  global-config:
    db-config:
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 开启下划线转驼峰（确保数据库字段名能对应到 PO 类）
    map-underscore-to-camel-case: true